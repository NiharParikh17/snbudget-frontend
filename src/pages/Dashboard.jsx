/**
 * Dashboard — top-level landing tab inside the authenticated app shell.
 * Placeholder until the real overview (budgets, recent activity, balances)
 * is wired up.
 */
function Dashboard() {
  return (
    <section className="px-4 sm:px-6 py-10">
      <h1 className="text-3xl font-bold tracking-tight text-slate-900 dark:text-white">
        Dashboard
      </h1>
      <p className="mt-3 text-slate-600 dark:text-slate-300">
        Your at-a-glance view of budgets, recent activity, and balances will
        live here. Coming soon.
      </p>
    </section>
  )
}

export default Dashboard

