# Base URL of the SNBudget Identity API. Defaults to http://localhost:8081
# when the variable is not set. The dev backend must allow this origin and
# send `Access-Control-Allow-Credentials: true` because the frontend uses
# `credentials: 'include'` so the HttpOnly refresh cookie can be sent.
VITE_API_BASE_URL=http://localhost:8081

